#pragma once

// Copy this file to secrets.h (gitignored - never committed) and fill in
// your real WiFi credentials there. config.h includes secrets.h directly.
#define WIFI_SSID "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
